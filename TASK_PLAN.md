# 金融站点 Demo 任务实施规划

## 1. 目标与范围
- 目标：交付一个可运行的单页 Demo，展示 K 线、扣非净利润、PE(TTM)、巴菲特式内在价值趋势线。
- 范围：先完成单股票（如 `600519`）端到端流程，再扩展到多股票。
- 非目标：用户系统、交易功能、复杂权限、全量回测平台。

## 2. 里程碑与交付物

### M1：数据抓取与落库（D1-D2）
**交付物**
- `backend/fetch_data.py`
- `backend/data/*.csv`（price/fundamentals/valuation）
- 字段口径说明文档

**任务**
1. 接入 AkShare 抓取日 K 数据。
2. 接入财报数据（扣非净利润相关字段）。
3. 获取或计算 PE(TTM) 数据。
4. 统一日期格式、股票代码格式。
5. 建立失败重试与本地缓存机制。

### M2：指标计算与时序对齐（D3）
**交付物**
- `backend/calc_iv.py`
- `backend/data/iv.csv`
- 计算参数配置（`r/g/N/gt`）

**任务**
1. 实现简化 Owner Earnings 计算：`OCF - CapEx`（MVP 近似）。
2. 实现简化 DCF 价值计算并输出每期内在价值。
3. 财报季度值前向填充到交易日频率。
4. 生成统一 API 消费格式。

### M3：后端 API（D4）
**交付物**
- `backend/app.py`（FastAPI）
- 本地运行脚本（`uvicorn` 命令）

**任务**
1. `GET /api/demo/series?code=...` 返回图表数据。
2. 增加参数化重算接口（可选）：`POST /api/demo/recalc`。
3. 增加健康检查：`GET /healthz`。

### M4：前端 Demo 页（D5-D6）
**交付物**
- `frontend/index.html`
- `frontend/app.js`
- `frontend/style.css`

**任务**
1. 用 ECharts 绘制 K 线主图。
2. 叠加 PE(TTM) 与内在价值线。
3. 副图展示扣非净利润柱状图。
4. 支持 dataZoom、十字光标联动。

### M5：移动端适配与验收（D7）
**交付物**
- 移动端样式适配
- 验收清单与问题清单

**任务**
1. 响应式布局与图表高度策略。
2. 小屏 legend 精简与参数面板折叠。
3. 横屏优化与性能检查。

## 3. 执行策略
- 采用“预计算 + 按需重算”混合模式：
  - 预计算：基础行情、默认参数内在价值线。
  - 实时重算：用户调整 `r/g/N/gt` 时触发。
- 缓存策略：
  - 结果键：`code+date_range+params_hash`
  - 默认 TTL：15 分钟（Demo 可调）。

## 4. 验收标准（Definition of Done）
1. 打开 Demo 页可见完整图表（K 线 + 三条指标）。
2. 时间窗口切换不触发重算，仅做切片展示。
3. 参数变更可触发重算并回显新曲线。
4. 移动端（375px 宽）可操作且无明显卡顿。
5. 核心接口平均响应时间：
   - 读缓存 < 300ms
   - 冷启动计算 < 3s（单票）

## 5. 风险与预案
1. **AkShare 字段变化**：建立字段映射层与空值兜底。
2. **财报口径差异**：界面增加口径说明与版本信息。
3. **计算波动争议**：开放参数并提供默认模板（保守/中性/乐观）。
4. **移动端性能**：分段加载、采样点限制、禁用非必要动画。

## 6. 下一步（立即执行）
1. 初始化 `backend/` 与 `frontend/` 目录。
2. 先跑通单股票 `600519` 的数据抓取和 API 输出。
3. 接入前端最小图表并完成首版截图。
