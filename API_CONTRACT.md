# API Contract: `GET /api/demo/series?code=...`

本文档定义 `GET /api/demo/series` 的稳定响应结构与时序对齐规则，供后端、前端与数据处理流程共同遵循。

## 1) 响应格式（JSON）

```json
{
  "code": "600519.SH",
  "range": {
    "start": "2019-01-01",
    "end": "2026-05-20"
  },
  "series_version": "v1",
  "dates": ["2026-05-18", "2026-05-19", "2026-05-20"],
  "kline": [
    [1668.0, 1675.5, 1659.3, 1682.0, 5234567],
    [1676.0, 1669.2, 1661.8, 1680.5, 4983210],
    [1669.0, 1672.8, 1660.0, 1679.0, 4765432]
  ],
  "pe_ttm": [29.31, null, 29.45],
  "adj_np": [74780000000, 74780000000, 74780000000],
  "intrinsic_value": [2010.4, 2008.7, 2012.3],
  "params": {
    "r": 0.1,
    "g": 0.03,
    "N": 10,
    "gt": 0.02
  },
  "source_meta": {
    "market_data": "vendor_x_daily",
    "financial_data": "vendor_y_quarterly",
    "valuation_model": "dcf_v2",
    "timezone": "Asia/Shanghai",
    "last_updated_at": "2026-05-20T15:30:00+08:00"
  }
}
```

---

## 2) 顶层字段定义

### `code`
- 证券代码（字符串）。
- 与请求参数 `code` 一致。

### `range`
- 返回区间对象。
- 结构：`{ "start": "YYYY-MM-DD", "end": "YYYY-MM-DD" }`。
- 表示当前响应中时序数据覆盖的自然日边界。

### `series_version`
- 序列契约版本（字符串），用于前后端兼容控制。
- 当前版本：`v1`。

---

## 3) 时间轴

### `dates`
- 交易日序列，格式 `YYYY-MM-DD`。
- 严格升序。
- 作为所有时序字段的对齐基准。

---

## 4) K 线字段

### `kline`
- 二维数组，与 `dates` **等长**。
- 每个元素为固定顺序的 5 元组：

```text
[open, close, low, high, volume]
```

- 含义与类型：
  - `open`: 开盘价（number）
  - `close`: 收盘价（number）
  - `low`: 最低价（number）
  - `high`: 最高价（number）
  - `volume`: 成交量（number，单位按数据源定义）

---

## 5) 财务与估值序列

以下字段均与 `dates` **同长度**，并按索引一一对应。

### `pe_ttm`
- 市盈率（TTM）序列。
- 缺失值必须返回 `null`（不可用 `0`、空字符串或省略）。

### `adj_np`
- 扣非净利润序列（一般由季度财报披露值映射到日频）。
- 使用“披露日映射 + 前向填充”规则（见“时序对齐原则”）。

### `intrinsic_value`
- 内在价值序列（日频）。
- 缺失值返回 `null`。

---

## 6) 元信息

### `params`
- 参数快照对象，记录生成估值序列时使用的关键参数：

```json
{ "r": 0.1, "g": 0.03, "N": 10, "gt": 0.02 }
```

- 字段说明：
  - `r`: 折现率
  - `g`: 显性期增长率
  - `N`: 显性期年数
  - `gt`: 永续增长率

### `source_meta`
- 数据来源与更新时间元信息。
- 建议至少包含：
  - `market_data`: 行情数据源标识
  - `financial_data`: 财务数据源标识
  - `valuation_model`: 估值模型版本
  - `timezone`: 数据时区
  - `last_updated_at`: 最近更新时间（ISO 8601）

---

## 7) 时序对齐原则

为保证可复现性，以下规则固定并对外公开：

1. **财报（季度）到交易日：前向填充**
   - 先将季度财务值挂载到对应披露日映射后的交易日（见第 2 条）。
   - 从该交易日起向后逐日填充，直到下一次财报值生效。

2. **非交易日处理：映射到下一个交易日（固定规则）**
   - 当披露日或事件日落在非交易日时，统一映射到**下一个交易日**。
   - 该规则全局固定，不与“最近交易日”混用。

3. **空值策略：返回 `null`**
   - 若某日某字段无有效值，接口返回 `null`。
   - 是否断线绘制、插值或隐藏，由前端自行决定。

---

## 8) 一致性约束（服务端必须满足）

- `dates`、`kline`、`pe_ttm`、`adj_np`、`intrinsic_value` 长度必须一致。
- 任一序列不得在中间插入非对齐数据点。
- 所有日期字段必须为 `YYYY-MM-DD`；时间戳字段使用 ISO 8601。
- 版本变更（新增/重命名/语义变化）需升级 `series_version`。
